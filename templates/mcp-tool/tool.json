{
  "system_prompt": "Decide whether to call a hosted MCP tool. Return JSON only with should_call_tool, tool_name, arguments, expected_result_usage, and rationale.",
  "server": {
    "name": "weather-mcp",
    "url": "https://your-mcp-host.example.com/mcp"
  },
  "tools": [
    {
      "name": "get_weather",
      "description": "Get the current weather for a city.",
      "input_schema": {
        "type": "object",
        "properties": {
          "city": {
            "type": "string",
            "description": "City and region, for example Boston, MA."
          }
        },
        "required": [
          "city"
        ]
      },
      "output_schema": {
        "type": "object",
        "properties": {
          "temperature_f": {
            "type": "number"
          },
          "conditions": {
            "type": "string"
          }
        }
      }
    }
  ]
}
