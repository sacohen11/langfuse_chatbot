{
  "name": "support_ticket_lookup",
  "description": "Look up a customer support ticket.",
  "input_schema": {
    "type": "object",
    "properties": {
      "ticket_id": {
        "type": "string",
        "description": "Support ticket id."
      }
    },
    "required": ["ticket_id"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "status": {
        "type": "string",
        "description": "Ticket status."
      }
    },
    "required": ["status"],
    "additionalProperties": true
  }
}

